# Copy this file to .env (in the project root) and fill in the real value below. .env is
# gitignored - never commit the actual secret.

# Client secret for the Entra ID app registration used to send/fetch email via Microsoft Graph
# (see graph: in config.yaml for the tenant_id/client_id/mailbox that go with it).
GRAPH_CLIENT_SECRET=
